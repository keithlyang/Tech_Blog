
{{!-- {{#each posts as |post|}}
{{> post-info post }}
</div>
{{else}}
<h6 class="pb-4 has-text-centered is-italic">there are currently no posts</h6>
{{/each}} --}}

<h2>Test</h2>

{{#each post}}
    <h1>{{title}}</h1>
    <p>{{post_content}}</p>
    <h2>Comments:</h2>
    {{#each comments}}
        <p>{{comment_text}}</p>
    {{/each}}
{{/each}}

<button> Add post</button>